language: bash

# Default linux jobs
os: linux
sudo: required
dist: trusty

cache:
  directories:
    - $HOME/qemu

before_install:
  - make docker
  - make build
  - make env
  - make before_install
  
before_script:
  - make install
  - make after_install
  - make before_script

script:
  - make script

after_success:
  - make after_success

# don't notify me when things fail
notifications:
  email: false
